#include <bits/stdc++.h>
using namespace std;

int main(){
    int qtd;
    while(cin >> qtd && qtd!=0){
        vector<int> vetor;
        for(int x=2; x<=qtd;x++){
            vetor.push_back(x);
        }

        for(int x=0; vetor.size()!=1; ){
            
        }
    }
    return 0;
}